{{ form_start(form) }}
    {{ form_row(form.Pseudo, {'label': 'Pseudo'}) }}
    {{ form_row(form.citation, {'label': 'Citation'}) }}
    {{ form_row(form.imageFile, {'label': 'Image de profil'}) }}
    <input type='submit' value='Amazing'>
    {{ form_end(form) }}
{{ form_end(form) }}

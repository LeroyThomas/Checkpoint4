{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.title, {'label': 'Pseudo'}) }}
    {{ form_row(form.description, {'label': 'Citation'}) }}
    {{ form_row(form.pictureFile, {'label': 'Image de profil'}) }}
    <input type='submit' value='Amazing'>
    {{ form_end(form) }}
    {{ form_end(form) }}
{% endblock %}

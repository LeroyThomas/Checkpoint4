{% extends 'base.html.twig' %}

{% block title %}Bienvenue !{% endblock %}


{% block body %}
    <nav class="navbar navbar-expand-lg navbar-light bg-vert">
        <img src="{{ asset('build/images/logoAmphore.svg') }}" width="100px" height="74px" alt="logo"/></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('place_index') }}">Lieux</a>
                </li>
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <div class="dropdown" >
                                <button class="btn btn-bleuOze dropdown-toggle" style="color: black" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    {{ app.user.email }}
                                </button>
                                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                                    <a class="dropdown-item" href="{{ path('app_logout') }}"><i class="fas fa-sign-out-alt"></i> Se déconnecter</a>
                                </div>
                        </div>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
    <h1>Le Domaine de Colchide </h1>


    {% if app.user %}
        <div class="welcome">
            <h4>Bienvenue <strong>{{ app.user.pseudo }}</strong>, met toi à l'aise</h4>
{#            <a class="btn btn-yellow" href="{{ path('profil_new') }}">Personnalise ton Profil</a>#}
            <div class="citation">
                <blockquote class="blockquote mb-0">
                    <p>{{ app.user.citation }}</p>
                    <footer class="blockquote-footer">{{ app.user.pseudo }} <cite title="Source Title">The manual</cite></footer>
                </blockquote>
            </div>
        </div>
    {% else %}
        {% include '/security/login.html.twig' %}
    {% endif %}

{#    {% if profil.pseudo %}#}
{#        <div class="card" id="presentationProfil">#}
{#            <div style="width: 18rem;">#}
{#                <img class="card-img-top" src="{{ asset('uploads/images/'~profil.image) }}" alt="Card image cap">#}
{#            </div>#}

{#            <div class="citation">#}
{#                <blockquote class="blockquote mb-0">#}
{#                    <p>{{ profil.citation }}</p>#}
{#                    <footer class="blockquote-footer">{{ profil.pseudo }} <cite title="Source Title">The manual</cite></footer>#}
{#                </blockquote>#}
{#            </div>#}
{#        </div>#}
{#    {% endif %}#}
    <hr>

        <div class="carte" style="background-image:url('{{asset('build/images/carte.jpg')}}')">
{#            {% for place in places %}#}
                <div id="button1">
                    <a href="{{ path('place_show' , {'id': 9}) }}" class="btn btn-rouge">Acropole</a>
                </div>
                <div id="button2">
                    <a href="{{ path('place_show' , {'id': 10}) }}" class="btn btn-rouge">Erechthéion</a>
                </div>
                <div id="button3">
                    <a href="{{ path('place_show' , {'id': 11}) }}" class="btn btn-rouge">Parthénon</a>
                </div>
                <div id="button4">
                    <a href="{{ path('place_show' , {'id': 12}) }}" class="btn btn-rouge">Sparte</a>
                </div>
        </div>

{% endblock %}
